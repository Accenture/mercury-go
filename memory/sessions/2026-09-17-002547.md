# Session (2026-09-17T00:25:47.589Z)

**Agent:** Claude Code

## Replied to the mercury-composable team and upgraded their repo to v4.40.0

- **Reply** written to `/tmp/agent-memory-open-thread-staleness-reply.md` (the exchange path their report
  used): verdict, proposal-by-proposal mapping, the human-gate refinement and the acknowledgment rule
  (inspection ≠ use; a re-affirmation entry is the only reset), what their next review's step 8 does, their
  current lint picture, the upgrade details, dogfood evidence, thanks and credit.
- **Mode B upgrade of `~/sandbox/mercury-composable`** per `ENABLE.md`: dry-run → consent (Eric's
  instruction) → `--apply` → the optional knob added to their policy → adapters re-synced → stamped 4.40.0 →
  reconcile converged; their memory-lint (now v4.40.0) 0 errors, Python/Node identical; lightweight session
  log in their `memory/`; PR Accenture/mercury-composable#407 (branch `agent-memory-v4.40.0`, house shape;
  the EMU account refused again → `acn-ericlaw`). Their lint names three stalled unchecked threads for the
  next review's gate — two `(blueprint)` gaps at 131 sessions, one at 110 — and five completed threads
  (sslu 110–185) awaiting the sweep; Eric's on-command review there on 2026-09-16 had already closed several
  by hand.
- **Gap surfaced (Core Rule 5):** the reconcile re-installed `.agent/secret-scan-ignore` — a `seed-copy` row
  copies whenever the target lacks the file — although the team had deleted it deliberately on 2026-09-12
  (guard at zero exceptions). Harmless this time (the seed carries zero patterns) but it will re-offer on
  every upgrade: `seed-copy` cannot express "deliberately absent". New thread
  `ot-seed-copy-deliberate-absence`; flagged in their PR and in the reply.
- PR #6 (release bookkeeping) merged by Eric; `main` synced; branch deleted.

## Memory References

- ot-seed-copy-deliberate-absence (created, tier: working)
- backlog-real-work-dogfood (referenced — field report → release → target upgrade → reply: the flywheel closed end to end)
- upgrades-additive (referenced — the seed-copy gap must be fixed without ever deleting)
- target-repo-scope-only (referenced — the upgrade touched only the resolved target root)
- github-origin-mercury-go (referenced — PR-required rule; the acn-ericlaw account fact held again)
