# Session (2026-09-17T23:59:56.926Z)

**Agent:** Claude Code

## Shipped v4.41.0 (MINOR) — `[undeclared-reference]`: a fact edited without being declared

Eric asked for a review of `/tmp/agent-memory-undeclared-reference-check.md` (mercury-composable,
2026-09-17, on v4.40.1), then approved the build. Their instance 2: at their first closure gate the
maintainer closed two Blueprint gaps; the closing session (`2026-09-17-020650`) rewrote both records
across five commits and declared neither, so `refresh-metadata` read the decision as non-use and
`[overdue]` proposed sweeping them. **Verified in their repo** (commits present; the log names both ids
in prose only; `640de24f` changed footer lines only). It ended safely — their `15d10d3b` declared both
ids retroactively — but only because the maintainer asked about the warnings.

**Measured, not assumed:** a prototype of the check over the last 60 memory commits of three repos —
fact-editing commits staging a log alongside: 56/60, 59/60, 57/60; undeclared edits or closures found in
every repo (mercury-composable 10 across 5 commits incl. the original `thread-redis-kafka-rpc` closure;
mercury 2 incl. a core invariant; mercury-go 4 incl. three edits to `upgrades-additive`); and a
false-positive class the note did not foresee — condensing already-closed records at review (6, 6, 15
instances) — plus the v4.39.0 verbatim moves.

**Our own defect owned:** REVIEW.md step 10 (v4.40.0) said to list a stalled thread "only when the human
re-affirmed it"; read literally, a gate closure goes undeclared — which is what their log did. A closure is
a use: the close record is the completion event that makes the sweep counter honest.

**Built:** memory-lint check 16 `[undeclared-reference]` — pure core (`check_undeclared_references`) fed
by git plumbing (`--staged` for the pre-commit fragment; `--range BASE [HEAD]` for the CI floors; the first
memory-lint mode that calls git: stdlib `subprocess` / `node:child_process`, still agent-/git-/CI-invoked,
no daemon — `no-build-step-agent-run` consulted and intact). Counted: body edits, closures (`[ ]`→`[x]`),
new blocks in continuity, thread files and vision.md. Not counted: footer-only lines, condensation of an
already-closed record, verbatim moves (blank lines are layout, not substance), deleted blocks; silent when
no log is staged; advisory; message names the remedy. Both runtimes at parity; 11 mirrored tests each
(suites 74 → 85), including a real-git end-to-end case. Hook fragment runs `--staged` (advisory, stderr,
never blocks); all three CI floors run `--range base HEAD` after the changed-config scan.
**Field replay:** `903694f0`, `41bac544`, `9ecf2dcf` → flagged as closures; `640de24f` (refresh) and
`6b7b35f1` (review with condensation) → clean; our `d924230f` (condensation) → clean. Hook e2e in a
scratch repo: advisory printed, exit 0; declared → silent.

**Wording fixed:** REVIEW.md step 5 (completion age = the closing reference; an `[overdue]` on a thread
closed within the window → declare retroactively, don't sweep), step 8 (declare every disposition), step
10 (acted on: re-affirmed or closed; merely read is not a use); DECAY.md §2 (`Closed` event + the check's
rationale) and §6; protocol (both copies) tracks referenced/created/reactivated/closed and declares each
closed thread → Semantic steps row; schema `Closed:` line. Lockstep: SKILL.md, hooks README, ENABLE, docs
(built-in skills = sixteen checks, reliability), MANIFEST row, UPGRADE row + rung, CHANGELOG, README
table (4.35.0 trimmed), VERSION 4.41.0. This commit is the first live run of the check on our own repo: it
edits `stalled-thread-closure-gate`, which this log declares.

## Memory References

- stalled-thread-closure-gate (referenced — substance updated: a closure is declared; the v4.40.0 wording corrected)
- backlog-real-work-dogfood (referenced — third field note in two days, shipped the same day)
- no-build-step-agent-run (referenced — memory-lint now calls git in two modes; agent/git/CI-invoked, nothing daemonizes)
- github-origin-mercury-go (referenced — PR-required rule; the acn-ericlaw account fact applied)
