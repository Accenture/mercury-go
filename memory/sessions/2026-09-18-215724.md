# Session (2026-09-18T21:59:30.360Z)

**Agent:** Claude Code

## What happened

**v4.41.0 shipped and rolled out (since the previous log).** PR #9 merged as `9e33c3e`; tagged
`v4.41.0` (lightweight, on the merge commit) and published the release under the `acn-ericlaw` account
on Eric's go. The merge-commit floor run was the first live run of the new range check on this repo:
`undeclared-reference check: ok`. Mode B upgrades of the four family repos to 4.41.0 (hook surface
inspected stock 4.40.1 → `--apply --pre-apply-complete`; protocol re-copied — every target was
byte-identical to the 4.40.1 template; rung step 3 found no field shape anywhere; `--staged` ok on each
upgrade commit): mercury-composable #413, mercury #280, mercury-python #29, mercury-nodejs #97 — all
merged, and the post-merge floor printed `undeclared-reference check: ok` in all four. Two operational
lessons: (1) mercury-composable had a feature branch checked out with uncommitted work, so the upgrade ran
in a throwaway git worktree of `origin/main`; (2) the reconcile reads no forge from a worktree's `.git`
pointer file, treats the forge as unknown and additively seeds the GitLab floor — pass `--forge github`
in a worktree (the reconcile's own note says so); the three seeded files were removed and every re-copied
file byte-compared to its MANIFEST source.

**Field note reviewed: the `thread-` filename prefix (mercury-composable, 2026-09-18, cosmetic).**
Verified against v4.41.0: exactly one production line per runtime depends on the prefix (memory-lint
check 12's filename expectation); `refresh-metadata`, `archive-fact` and the `[undeclared-reference]`
surface matcher glob `*.md` and key on the footer id, so a file rename would cost no usage history.
The doc footprint is ~22 lines across 15 lockstep surfaces plus ~30 shell-test fixture lines — small,
not one line. The design record gives no rationale for the prefix (it argues only for stable id
filenames over timestamps). The in-flight-branch hazard (two files for one thread) is caught by
`[duplicate-id]` **before** merge on GitHub — the floor runs on `pull_request`, i.e. the PR's merge
commit — and after merge on GitLab source-branch pipelines and local merges. The stutter is
near-universal: this repo 21 of 36 thread files, mercury-composable 14/14, mercury 5/7, mercury-python
9/10, mercury-nodejs 7/9 — and mostly the tool's doing: `REVIEW.md` step 8 mandated the gate id
`ot-close-stalled-threads-<date>` in every repo, this repo's `ot-` habit was copied, and the
evolving-memory example used `thread-` ids (`thread-idempotency-keys` …) — the source of their
`thread-thread-…`. Eric's ruling, already recorded in mercury-composable's continuity (2026-09-18): a
thread id names the thing, never its kind; an existing thread is never renamed.

**Eric's decisions:** agree with the assessment; no retrospective rename of files; ship the guidance
patch as v4.41.1; the prefix drop is backlogged; no id migration ever (their arithmetic: an id declared
in 12 immutable logs would collapse to one use and decay while live).

**Built v4.41.1 (PATCH, guidance only).** `DECAY.md` §1 "Assigning an id" carries the rule, the
reason and the never-rename rationale; the schema template's footer legend and `memory/open-threads/`
section carry it (the "filename never changes" sentence is unchanged); `REVIEW.md` step 8 names new
gates `close-stalled-threads-<YYYYMMDD>` (a gate raised earlier keeps its `ot-` id; no code keys on
the gate id) and the summary example follows; `ENABLE.md`'s metadata-footer step names the rule; the
evolving-memory example's three `thread-…` fixture ids renamed consistently across its six files (a
curated documentation fixture, not a live repo's immutable log). Lockstep: `UPGRADE.md` row +
`4.41.0 → 4.41.1` rung, `CHANGELOG.md`, README table (4.36.0 row rotated out), `VERSION` 4.41.1.
No protocol text change → no Semantic steps row; a target converges by re-copy of `DECAY.md`,
`REVIEW.md` and `.agent/schema.md` (dry-run against mercury-python: exactly those three + stamp).
Memory: Key Decision `id-naming-no-kind-prefix`; parked backlog thread `open-threads-filename-prefix`
(the note's migration analysis preserved: pure file rename with ids untouched, the schema sentence, the
in-flight-branch case, and why additive-only was rejected).

**Verification:** lint suites 85/85 in both runtimes (no check changed); reconcile suites 38/38 both;
`--check-manifest` OK; the three shell suites pass; repo lint 0 errors, 7 warnings (the standing tier
drift, unchanged); Python/Node lint output identical; no residual kind-prefixed id in `examples/`.

## Memory References

- Created: `id-naming-no-kind-prefix` (tier: working), `open-threads-filename-prefix` (tier: working)
- Referenced: `stalled-thread-closure-gate` (the gate id in REVIEW.md step 8 is its mechanism — renamed
  for new gates, existing gates untouched), `github-origin-mercury-go` (release and PRs created under
  the `acn-ericlaw` account)
