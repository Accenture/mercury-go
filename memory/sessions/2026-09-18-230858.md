# Session (2026-09-18T23:08:58.720Z)

**Agent:** Claude Code

## What happened

**v4.41.1 shipped and rolled out (since the previous log).** PR #10 merged as `e8b27ea`; tagged and
published `v4.41.1` on Eric's go; the four family repos upgraded (mercury-composable #419, mercury #281,
mercury-python #30, mercury-nodejs #98 — all merged; post-merge floors green). One incident, owned: the
mercury-composable checkout had an active session; a "clean, on main" snapshot went stale within one second
of my helper's `checkout -b`, so the upgrade commit landed on that session's feature branch and swept its
in-progress `pom.xml` edit into it. The session recovered by resetting its branch; the upgrade was rebuilt
in an isolated worktree on the existing branch (`--forge github`) without touching the checkout. Rule
adopted: a repo that may have an active session gets the worktree path unconditionally.

**Two field reports reviewed and consolidated into v4.41.2 (PATCH).**

1. *Phantom fact* (mercury-composable, with a tested patch): the seeded `continuity.md` header shows the
   footer format as a literal example inside an inline code span; `parse_footers` matched it, so every
   enabled repo carried a phantom `kebab-id` fact — `tier: active`, unpinned, counted toward
   `[continuity-bloat]` — that raised nothing itself (never referenced → no `[overdue]`/`[stale-metadata]`;
   `refresh-metadata` preserves never-referenced footers). In the field it fired `36 > 35` at a true count of
   35, summoning a review with nothing to archive. Reproduced on copies of mercury (17→16), mercury-python
   (23→22) and mercury-nodejs (21→20) with the one header line neutralized; no other warning changed;
   mercury-composable had already applied the `<kebab-id>` stopgap on main, and this repo never had the
   seeded header. Fix as proposed, plus consistency: a backtick-wrapped footer is documentation to
   `parse_footers`, to the `[undeclared-reference]` block mapper and to `archive-fact`'s locator (both
   runtimes); blockquote position is not the test (every Vision footer is blockquoted and real). Seeded
   header now reads `id: <kebab-id>`. Patched lint on the three copies gives exactly the neutralized counts;
   this repo stays at 48.
2. *ADR proposals* (mercury-composable, guidance): "propose a newer ADR … wait for approval" read literally
   writes a non-decision into the decision record; their ledger held five `Proposed` ADRs for shipped
   decisions, and a withdrawn proposal has no honest status. Eric's rulings: the ledger records decisions
   only, entries on acceptance; proposals in the repo's register; prefix `RFC-` over `P-` (industry marker
   for a proposal open to comment; RFC → ADR pairing) and the file named after it — `docs/arch-decisions/RFC.md`.
   Nothing in the tool reads ADR status → guidance only. Field shape: mercury's ledger holds six `Proposed`
   ADRs (a one-time human cleanup, noted in the rung); this repo's ledger holds none, its lifecycle line
   updated.

**Built:** memory-lint `_in_inline_code`/`in_inline_code` guard in `parse_footers` and an `_id_line`/`id_line`
helper for `fact_blocks`; archive-fact `footer_line_index` guard; four mirrored lint tests per runtime (suites
89) and one archive-fact test each (11 / 10); template header placeholder; protocol ADR paragraph in both
copies (→ Semantic steps row); `DECAY.md` §12; schema ADR section (status vocabulary
`Accepted → Superseded / Deprecated`, the register, the gate writes the ADR on approval); memory-lint
`SKILL.md` note; our `docs/arch-decisions/ADR.md` lifecycle line. Lockstep: `UPGRADE.md` row +
`4.41.1 → 4.41.2` rung, `CHANGELOG.md`, README table, `MANIFEST.md` semantic row, `VERSION` 4.41.2.

**Verification:** lint suites 89/89 both runtimes; archive-fact 11 / 10; refresh-metadata 6 / 6; reconcile
38/38 both; three shell suites pass; `--check-manifest` OK; repo lint 0 errors, 7 warnings (unchanged tier
drift), Python/Node identical; dry-run against mercury-python: DECAY.md, schema, memory-lint and archive-fact
built-ins + the protocol semantic step + stamp.

## Memory References

- Created: `adr-ledger-decisions-only` (tier: working)
- Referenced: `github-origin-mercury-go` (release and PR created under the `acn-ericlaw` account),
  `id-naming-no-kind-prefix` (the RFC/ADR id sequences follow the same naming stance: separate, permanent ids)
