# Session (2026-09-18T23:20:13.067Z)

**Agent:** Claude Code

## What happened

**Two maintainer gates on the new register, both exercised on PR #11's branch.**

1. **RFC-0004 promoted → ADR-0008.** Eric accepted recording the ledger rule itself as an ADR. Written
   into `docs/arch-decisions/ADR.md` (newest first, `formalizes: adr-ledger-decisions-only`, with the
   alternatives weighed — `Proposed` inside the ledger, no register at all, a repo-named register — and the
   trade-offs); the continuity fact's title gained its `(ADR-0008)` tag; RFC-0004 is marked
   `Promoted → ADR-0008 (2026-09-18)` and stays in the register as the pointer. The first proposal to travel
   the path it describes.
2. **RFC-0005 raised — an opt-in seed for the governance pair.** Eric's proposal: ship sample
   `templates/docs/arch-decisions/ADR.md` + `RFC.md` skeletons. Assessment given and accepted: opt-in only
   (`ENABLE.md` asks once at enable; Mode B never re-offers), which needs a new MANIFEST policy `optional` —
   RFC-0002's option (b) made concrete — so it is a MINOR release, **v4.42.0**, after 4.41.2 ships; plain
   `seed-copy` was rejected because it would install governance ceremony everywhere and re-offer the pair after
   a deliberate deletion. Backlog thread `governance-pair-opt-in-seed` created as the live work item, pointing
   at RFC-0005 (and RFC-0005 at it); the v4.41.2 changelog bullet updated.

## Memory References

- Created: `governance-pair-opt-in-seed` (tier: working)
- Referenced: `adr-ledger-decisions-only` (edited — `(ADR-0008)` tag; formalized by ADR-0008),
  `ot-seed-copy-deliberate-absence` (RFC-0005 builds on its option b — consulted, not edited),
  `vision-agent-memory` (both proposals trace to it)
