# Session (2026-09-19T00:04:18.064Z)

**Agent:** Claude Code

## What happened

**v4.42.0 rolled out** (since the previous log): tagged and published; the four family repos upgraded through
the worktree path (mercury-composable #423, mercury #283, mercury-python #32, mercury-nodejs #100 — all merged;
each a schema re-copy + stamp, the governance pair reported as two `optional` notes or `ok — adopted` where a
ledger already existed; no adopt anywhere). mercury-composable's active session stayed on its feature branch.

**Memory review (cadence — 11 sessions since 2026-09-16; Eric: "run the review").** Ran `REVIEW.md` from an
up-to-date `main` on its own branch. Steps 2–3 via `refresh-metadata` (26 footers: uses recounted, tiers
re-derived — e.g. `github-origin-mercury-go` uses 3→11, the three v4.41.x/v4.42.0 Key Decisions
working→active, ten completed thread records active→archive-candidate). Steps 4–5 via `archive-fact`: one
faded Key Decision and four completed threads past `archive_window` moved to `memory/archive/2026-Q3.md` +
INDEX (below). Step 6: lint clean — no over-archival, no id in both places. Step 7: invariants not due
(21 of 40 sessions since 2026-08-22). Step 8: no stalled thread (`[thread-stale]` silent; the oldest
unreferenced open threads sit at 18 sessions against a 40-session window). Contradiction/drift/shipped-items
scan over the seventeen open threads: no contradiction, no altitude drift; `ot-seed-copy-deliberate-absence`
gains a progress line (v4.42.0 delivered its option b — the `optional` policy — and narrowed the question);
`ot-dogfood-backfill-optional` stays open (grandfathered Key Decisions still carry no footers);
`ot-graduation-followups` keeps item 2 (archive the pre-graduation repo — Eric's). Smoke test alongside:
12/12 from memory alone; result row appended. `[closed-thread-bloat]` cleared by the sweep (158 → 128 lines
of completed records, under the 150 budget); the remaining stubs are 5–10 lines each and were left as
written. Note for the maintainer: `git-hook-fragment-dispatch` archived as faded is the fact ADR-0007
formalizes — the ledger's `formalizes:` pointer still resolves through `archive/INDEX.md`; pin it `core` if it
should stay in the per-session read path.

## Memory Review (2026-09-18, through 2026-09-19-000416)
- Reactivated:   0
- Superseded:    0
- Archived:      1  fact → memory/archive/2026-Q3.md (faded): git-hook-fragment-dispatch (sslu 24)
- Swept threads: 4  completed Open Threads → archive (faded, past archive_window 20): bp-reconcile-target-state (22), onboarding-efficiency-v4380 (22), protocol-pointer-enterprise-activation (22), ot-reverify-invariants-20260814 (21 — crossed the window when this review's own log was written; swept in the same pass)
- Archive-verify: pass (memory-lint: no archived id referenced within archive_window; no id in both places; 0 errors)
- Tier changes:  refresh-metadata rewrote 26 footers — 5 working→active, 1 working→archive-candidate, 13 active→archive-candidate, 1 working→archive-candidate (the 2026-09-16 gate record), uses recounted throughout
- Invariants:    not due (next re-verify in 19 sessions)
- Stalled threads: none
- Promoted core: 0  (auto-core off; core is human-set)
- Smoke test:    12/12

## Memory References

- Referenced: `ot-seed-copy-deliberate-absence` (edited — v4.42.0 progress line), `github-origin-mercury-go`
  (the review lands by PR)
