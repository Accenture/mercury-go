# Session (2026-09-19T00:12:50.447Z)

**Agent:** Claude Code

## What happened

**Reactivated `git-hook-fragment-dispatch` and pinned it `core` (Eric, after PR #13 merged).** The
2026-09-18 review had archived the Key Decision as faded by the deterministic rule (24 sessions unreferenced,
not `core`) while ADR-0007 still formalizes it. Per `REVIEW.md` *Reactivation*: the block moved back from
`memory/archive/2026-Q3.md` into `continuity.md` under Key Decisions (its former place), its INDEX line
removed, `last_used` refreshed, `uses` incremented — and, on the maintainer's instruction, `tier: core`
(a human-set tier the review never overrides; `DECAY.md` §6/§8), with a one-sentence note recording the
pin. memory-lint: 0 errors — no id in both places; Python/Node identical.

## Memory References

- Reactivated: `git-hook-fragment-dispatch` (from the archive; pinned `core` by the maintainer)
- Referenced: `github-origin-mercury-go` (lands by PR)
