# Session (2026-09-18T23:16:56.013Z)

**Agent:** Claude Code

## What happened

**Exercised the v4.41.2 ledger rule on this repo before merging PR #11** (Eric: "exercise the new
agent-memory protocol for this repo so it gains a new RFC.md file"). Opened `docs/arch-decisions/RFC.md`
— the proposal register the protocol now names as its example — with its rules (separate `RFC-NNNN`
sequence; two recorded exits, promoted or withdrawn; never deleted; map-don't-duplicate against the
thread that holds the state; the human decides promotion) and four **real** proposals drawn from what this
repo already has under consideration, none invented:

- RFC-0001 — protocol-text propagation: keep the per-release Semantic row, make the protocol `verbatim`,
  or a hybrid keyed on a still-stock target copy (thread `ot-protocol-text-propagation`, open since
  2026-08-21).
- RFC-0002 — let `seed-copy` express "deliberately absent": a target-side tombstone or an `optional`
  policy, `upgrades-additive` intact either way (thread `ot-seed-copy-deliberate-absence`).
- RFC-0003 — drop the `thread-` filename prefix: parked on the maintainer's 2026-09-18 ruling; the
  additive accept-both variant recorded as rejected (thread `open-threads-filename-prefix`).
- RFC-0004 — formalize the ledger rule itself as ADR-0008 (`formalizes: adr-ledger-decisions-only`),
  with the honest alternative that it stays a Key Decision; the maintainer's gate — asked, not decided.

Each thread-backed proposal points at its thread and the thread gained a `→ proposal: RFC-NNNN` line
(the reasoning lives in the register, the state in memory — the same mapping an accepted ADR has with its
`(ADR-NNNN)`-tagged fact). `ADR.md`'s header points at the register; the continuity fact
`adr-ledger-decisions-only` records the adoption; README tree and the docs nav list `RFC.md`;
the v4.41.2 changelog entry gains the dogfood bullet. No new memory fact — the register is a document.

## Memory References

- Referenced: `adr-ledger-decisions-only` (edited — adoption note), `ot-protocol-text-propagation`
  (edited — RFC-0001 pointer), `ot-seed-copy-deliberate-absence` (edited — RFC-0002 pointer),
  `open-threads-filename-prefix` (edited — RFC-0003 pointer), `github-origin-mercury-go` (PR #11 is the
  vehicle)
