# Session (2026-09-18T23:47:56.133Z)

**Agent:** Claude Code

## What happened

**v4.41.2 shipped and rolled out (since the previous log).** PR #11 merged as `3abbe8e`; tagged and
published `v4.41.2`; the four family repos upgraded through the worktree path unconditionally
(mercury-composable #422, mercury #282, mercury-python #31, mercury-nodejs #99 — all merged; post-merge
floors green; live facts dropped by exactly one where the header literal existed: mercury 17→16,
mercury-python 23→22, mercury-nodejs 21→20). mercury's ledger holds three `Proposed` ADRs — the one-time
human cleanup, left to Eric. GitHub left mercury-nodejs's merged branch on origin; deleted to match.

**Built v4.42.0 — the opt-in seed for the governance pair (Eric: "start v4.42.0").** From RFC-0005 and
the backlog thread. Design fixed as a seventh MANIFEST policy, **`optional`** — offered, never imposed:
a row is installed only on an explicit `--adopt <target>` (repeatable; a path ending in `/` adopts every
optional row under it; an unmatched path is refused), an existing copy is never touched, an absent one is
listed as an `optional` note for reference and never counts as pending — so convergence is unaffected and
an upgrade never re-asks. Both reconcile runtimes (plan branch, `adopt` mechanical verb, `--adopt` parsing,
`adopt_matches`/`adoptMatches`), four mirrored tests each (suites 38 → 42), `--check-manifest` OK in
both. Skeletons `templates/docs/arch-decisions/ADR.md` + `RFC.md` — project-neutral, no placeholders, so
the mechanical copy is complete; MANIFEST policy definition, two rows and a row note. `ENABLE.md`: the
policy list, the never-does bullet, Mode B ("reference only — do not re-ask"), the Step 9 report line, the
Step 10 offer (d) with the exact command. Schema: the ADR section's install stance + a new `RFC.md`
section. Docs: `DESIGN-reconcile.md` (seven policies; never-does), the enable and upgrade guides, the
README templates tree. Register: RFC-0005 carries the implemented design (promotion to an ADR at the
maintainer's gate); RFC-0002 gains a progress note (option b now exists — which `seed-copy` rows move to it
stays open). Lockstep: `UPGRADE.md` row + `4.41.2 → 4.42.0` rung, `CHANGELOG.md`, README table,
`VERSION` 4.42.0. No protocol text change → no Semantic steps row.

**Verification.** Field dry-run against mercury-python: the two `optional` notes appear and the pending
count excludes them; Python/Node reports identical; on a scratch copy `--adopt docs/arch-decisions/` plans
two `adopt` lines, the Node CLI `--apply --adopt` installs both skeletons byte-identical to the templates,
the re-plan reports `ok — adopted, never touched`, and `--adopt docs/nope.md` is refused. Suites: lint
89/89 both runtimes; archive-fact 11 / 10; refresh-metadata 6 / 6; reconcile 42/42 both; three shell suites
pass; `--check-manifest` OK both; repo lint 0 errors, Python/Node identical.

**Memory.** Key Decision `governance-pair-optional-seed`; the backlog thread `governance-pair-opt-in-seed`
closed with a close record (lesson: the opt-in stance needed its own policy — `seed-copy` cannot express a
file a team deliberately does not have, and that is exactly what "offered, never imposed" is).

## Memory References

- Created: `governance-pair-optional-seed` (tier: working)
- Closed: `governance-pair-opt-in-seed` (shipped in v4.42.0 — the close record is the completion event)
- Referenced: `adr-ledger-decisions-only` (the pair implements the rule ADR-0008 formalizes),
  `ot-seed-copy-deliberate-absence` (RFC-0002 option b is now the `optional` policy — consulted, RFC note
  added), `upgrades-additive` (the new policy never rewrites or deletes — checked against it),
  `github-origin-mercury-go` (PR created under the `acn-ericlaw` account)
