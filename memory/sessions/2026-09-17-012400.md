# Session (2026-09-17T01:24:00.935Z)

**Agent:** Claude Code

## Shipped v4.40.1 (PATCH) — punctuation-tolerant secret exemptions; the waiver file as a last resort

Eric asked for a review of `/tmp/agent-memory-enum-punctuation-note.md` (mercury-composable, 2026-09-17, on
v4.40.0): a comment sentence mentioning a setting — `credentials.source=OAUTHBEARER,` — flagged
`[secret-material]` while the bare setting was exempt; the enum exemption is a full match with no tolerance
for the trailing punctuation that prose rides into the captured value, while the knob exemption twelve lines
above tolerates it on purpose. **Verified** by running the scanner on their reproduction in both runtimes.
**Wider than reported:** the probe showed every placeholder class except the knob failing the same way
(`password=changeme.`, `client.secret=${CLIENT_SECRET}.`, `api.key=<your-key-here>,`). Their one-regex fix
would have left those behind, and a naive strip would have broken `$(…)`, which is exempt today and ends in `)`.

**Fix shipped:** `_is_placeholder_value` / `is_placeholder_value` run the exemptions as-is, then once more
with trailing `).,` stripped. Mirrored test per runtime (their case in prose with all three marks, the three
sibling classes, the `$(…)` regression guard, a real value with punctuation that must still flag); suites
73 → 74 each; the reproduction now flags only the control line in both runtimes.

**Eric's directives, in order:** include the team's second suggestion — remove `.agent/secret-scan-ignore`
because it encourages the field to use it; "zero secret leakage is the goal, even dummy test values can
become false positive in field security scan" — then the refinement: treat the file as the **last resort,
an escape hatch the field should not exercise unless they know the implications.** Implemented as: not
seeded (MANIFEST seed-copy row + `templates/.agent/secret-scan-ignore` removed; ENABLE 5e step dropped), not
recommended (hook header + messages, `.githooks/README.md`, both PROTOCOL copies, memory-lint `SKILL.md`,
docs reworded to "restructure the fixture; last resort only knowing the implications"), mechanism unchanged
(hook + three CI floors honor a committed file; the hook now states the implication when it exempts one).
Nothing deprecated, nothing deleted in targets (`upgrades-additive` intact). Protocol text changed → the
mandated Semantic steps row. Reconcile tests moved their seed-copy example to the PR template.

**Verification:** lint suites 74/74 (py/mjs); reconcile suites 38/38 (py/mjs); `--check-manifest` OK in
both runtimes (20 semantic rows → 21, all gated ≤ 4.40.1); `tests/*.sh` pass; memory-lint on this repo 0
errors; Python/Node output identical on the reproduction.

**Memory:** Key Decision `secret-waiver-last-resort` created (Eric's ruling + rationale);
`ot-seed-copy-deliberate-absence` body updated — its motivating row is gone, the general seed-copy question
stays open. Not touched: the whitepaper mention (collateral refresh is periodic), the CI floors' waiver
loops (still valid — `tests/test_ci_secret_scan_waiver.sh` unchanged and green).

## Memory References

- secret-waiver-last-resort (created, tier: working)
- ot-seed-copy-deliberate-absence (referenced — body updated: motivating case resolved, general question open)
- upgrades-additive (referenced — the change adds no deletion to any target; the mechanism stays)
- backlog-real-work-dogfood (referenced — second field note in two days, both shipped the same day)
- github-origin-mercury-go (referenced — PR-required rule; account fact applied)
