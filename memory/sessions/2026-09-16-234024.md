# Session (2026-09-16T23:40:24.948Z)

**Agent:** Claude Code

## Shipped v4.40.0 (MINOR) — stalled open threads → human closure gate

Eric approved the build after the review (log `2026-09-16-231835`) and set the design direction:
"an open thread that has been stalled or neglected for a long while is a signal for closure";
rectify it through a **human gate**. Context he gave: during rapid development many competing
priorities mean loose ends get filed in an open thread and left behind without a trace. He is
running exactly this review in mercury-composable now, so the shape ships into a live exercise.

### What was built

- **`memory-lint` check 15 `[thread-stale]`** (Python + Node at parity, identical output verified
  by diff on this repo): an unchecked thread with refs-based `sessions_since_last_used` >
  `thread_stale_window` is *stalled*; a never-referenced thread counts from `created` (its seeded
  first use, per the schema) so no pinned thread is invisible. Advisory only; the pin is untouched;
  the message points at the gate ("the owner closes it, or re-affirms it under Memory References").
  New knob `thread_stale_window` (default 40 = `verify_invariants_every`) with fallback when absent.
  Six mirrored tests per runtime — knob default/parse, past/within window (strict `>`), unpinned
  ignored, never-referenced-from-created (+ undated → unmeasurable, left alone), thread-layer
  end-to-end through `load_repo`; suites 67 → 73 each, all green.
- **`REVIEW.md` step 8 — the human closure gate**: one `Close stalled threads (due)` gate thread
  per review (`ot-close-stalled-threads-<YYYYMMDD>`; extend an open gate rather than doubling),
  bodies re-read with the human, then the **human** decides per thread — close (`[x]` + 3–6-line
  record; undelivered items recorded as *deliberately dropped*; a `(blueprint)` closure is an
  altitude decision, §12) or re-affirm (a `## Memory References` entry with the reason — the only
  reset; the exception, not the default). The review never closes a thread. Stamp/Summarise
  renumbered 9/10; Summarise now states inspecting a stalled thread is **not** a use; the
  Contradiction backstop re-reads every unchecked body (all shipped → normal completion; anything
  undelivered → the gate); summary format gains `Stalled threads:`; "When it runs" gains the
  stalled-gating paragraph.
- **`DECAY.md`**: §5 rule 4 "pinned is not unwatched"; §6 callout now covers the third never-decay
  class + the **one-lifecycle-per-record** rule (a sub-list inside a thread or a Project-State
  scalar cannot carry an independent commitment — both stale records in the report had that shape).
- **Schema**: `status` field note (never a version history, never a commitment), thread lifecycle
  note, knob list; **doc fix**: the invariant-verify pointer said "step 6" in REVIEW/DECAY/schema
  since the archival-verify step was inserted before it — corrected to 7.
- **Lockstep**: memory-lint `SKILL.md` (description unchanged → no adapter re-sync needed),
  decay-policy ×3 (this repo, template, example fixture), docs (decay-parameters, concepts/decay,
  built-in-skills — which had also been missing checks 12–14 since v4.39.0; review guide flow +
  table; index.md status note → v4.40.0), MANIFEST Semantic steps row (optional knob + first gate;
  `--check-manifest` OK), UPGRADE ladder row + `4.39.2 → 4.40.0` rung, CHANGELOG, README table
  (4.40.0 added, 4.34.1 trimmed to keep 10; graduation callout reworded to past tense),
  `VERSION` → 4.40.0. Not touched: `memory/PROTOCOL.md` (no protocol text change → no propagation
  obligation), whitepaper/deck (collateral refreshes are periodic, still say v4.39.2).

### Verification

Both lint suites green (73/73 each); `python3`/`node` memory-lint output byte-identical; on this repo
the new check fires exactly for the two expected threads — `bp-sdlc-overlay` sslu 155 and
`vbdi-lifecycle-direction` sslu 92 (refs-based; my earlier footer-based estimate said 151/89); the
three `tests/*.sh` suites pass (waiver suite static-only on bash 3.2, as designed);
`scripts/reconcile.py --check-manifest` → manifest OK.

### Memory

- Key Decision `stalled-thread-closure-gate` created (Eric's design decision + rationale).
- `ot-stalled-thread-closure-gate` closed with a 6-line stub (shipped v4.40.0; PR pending).
- The two stalled threads are **not** listed below — inspecting them as gate evidence is not a use
  (the rule shipped in this release); the gate is raised at this repo's next review, and Eric
  decides their fate there.
- Durable lesson: every never-decay class needs a human re-check — "never-decay ≠ never-checked"
  was already the house principle; the gap was applying it to the class the principle sat beside.

Next: Eric commits on a branch and opens the PR (org-home PR rule; What/Why from this log), tags
`v4.40.0` in the house format, and replies to the mercury-composable team — their upgrade converges
by re-copy, and their in-flight review is the first live exercise of step 8.

## Memory References

- stalled-thread-closure-gate (created, tier: working)
- ot-stalled-thread-closure-gate (referenced — closed this session)
- vision-agent-memory (referenced — the gate serves faithful, traceable delivery)
- never-pick-a-winner (referenced — the gate's governing invariant: the tool surfaces, the human decides)
- no-build-step-agent-run (referenced — the advisory is an agent-invoked helper; nothing daemonizes)
- upgrades-additive (referenced — targets converge by verbatim re-copy; the knob is optional)
- backlog-real-work-dogfood (referenced — field report → release, the flywheel)
- github-origin-mercury-go (referenced — PR required for the org home)
